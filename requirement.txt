# Web framework
Flask>=2.0
Flask-SQLAlchemy>=2.5
python-dotenv

# Email handling
imapclient
email

# ML and data processing
pandas>=1.3
numpy>=1.21
scikit-learn>=1.0
joblib>=1.0

# HTML sanitization and parsing
bleach>=5.0
beautifulsoup4>=4.10
lxml>=4.6

# Template rendering
Jinja2>=3.0
Werkzeug>=2.0

# Document and image processing (for sandbox viewing)
PyMuPDF
pdfminer.six
python-docx
Pillow
python-magic

# For production deployment
gunicorn

# Optional: Docker control via Python (if applicable)
docker
